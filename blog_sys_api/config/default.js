const config = {
  // 启动端口
  port: 3003,

  // 数据库配置
  database: {
    DATABASE: 'db_blog',
    USERNAME: 'root',
    PASSWORD: 'feng-1327',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config